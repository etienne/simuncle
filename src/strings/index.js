import { default as intro } from './intro.csv';
import { default as config } from './config.csv';
import { default as strings } from './strings.csv';

const localStrings = { intro, config, strings };

export default localStrings;